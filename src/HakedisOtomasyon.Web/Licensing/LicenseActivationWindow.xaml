<Window x:Class="HakedisOtomasyon.Web.Licensing.LicenseActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uygulama Aktivasyonu"
        Width="520" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F7FA"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True">

    <Window.Resources>
        <!-- Ana buton stili -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B0BEC5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sekonder buton (kopyala) -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#ECEFF1"/>
            <Setter Property="Foreground" Value="#455A64"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B0BEC5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox stili -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CFD8DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label stili -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#546E7A"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="36,28,36,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <StackPanel Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="Uygulama Aktivasyonu"
                       FontSize="22" FontWeight="Bold"
                       Foreground="#1A237E"/>
            <Rectangle Height="2" Fill="#1565C0" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Alt açıklama -->
        <TextBlock Grid.Row="1"
                   Text="Bu uygulamayı kullanabilmek için cihazınıza özel aktivasyon kodu girmeniz gerekmektedir."
                   TextWrapping="Wrap"
                   FontSize="12"
                   Foreground="#607D8B"
                   Margin="0,10,0,20"/>

        <!-- Firma Adı -->
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Style="{StaticResource FieldLabel}" Text="Firma Adı"/>
            <TextBox x:Name="TxtFirmaAdi"
                     Style="{StaticResource ModernTextBox}"
                     MaxLength="100"/>
        </StackPanel>

        <!-- Cihaz Kodu -->
        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Style="{StaticResource FieldLabel}" Text="Cihaz Kodu"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtDeviceCode"
                         Style="{StaticResource ModernTextBox}"
                         IsReadOnly="True"
                         Background="#ECEFF1"
                         FontFamily="Consolas"
                         FontSize="14"
                         FontWeight="Bold"
                         Foreground="#1A237E"/>
                <Button Grid.Column="1"
                        x:Name="BtnCopy"
                        Content="📋  Kopyala"
                        Style="{StaticResource SecondaryButton}"
                        Margin="8,0,0,0"
                        Click="BtnCopy_Click"/>
            </Grid>
        </StackPanel>

        <!-- Aktivasyon Kodu -->
        <StackPanel Grid.Row="4" Margin="0,0,0,14">
            <TextBlock Style="{StaticResource FieldLabel}" Text="Aktivasyon Kodu"/>
            <TextBox x:Name="TxtActivationCode"
                     Style="{StaticResource ModernTextBox}"
                     FontFamily="Consolas"
                     FontSize="12"/>
        </StackPanel>

        <!-- Hata / Başarı mesajı -->
        <Border Grid.Row="5"
                x:Name="MessageBorder"
                Visibility="Collapsed"
                CornerRadius="5"
                Padding="12,8"
                Margin="0,0,0,12">
            <TextBlock x:Name="TxtMessage"
                       TextWrapping="Wrap"
                       FontSize="12"
                       FontWeight="SemiBold"/>
        </Border>

        <!-- Aktive Et butonu -->
        <Button Grid.Row="6"
                x:Name="BtnActivate"
                Content="Aktive Et"
                Style="{StaticResource PrimaryButton}"
                Height="44"
                Click="BtnActivate_Click"/>
    </Grid>
</Window>
